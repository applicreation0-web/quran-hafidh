# Qushayri note-call research note

Research-only record for 0.10.5. The two residual `page,number` pairs from the v5 textual detector — `(60,5)` and `(242,5)` — are not footnote calls. In the pinned source they are the decimal value `2.5 percent`; the genuine footnote calls on those passages are respectively 25/26 and 376/377. The independent superscript-span cross-check must therefore exclude a digit immediately preceded by a decimal point whose preceding character is also a digit. No production corpus data is changed by this note.
