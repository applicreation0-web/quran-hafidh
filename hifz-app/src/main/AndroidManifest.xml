<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quran Hifz is local-first and does not need the broad device/network permissions
         declared transitively by the Onyx SDK. Keep the final APK scope minimal. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH" tools:node="remove" />
    <uses-permission android:name="android.permission.DUMP" tools:node="remove" />

    <application
        android:allowBackup="false"
        android:label="Quran Hifz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        tools:replace="android:allowBackup,android:label">
        <activity android:name=".ui.HifzSessionActivity" android:exported="false" />
        <activity android:name=".ui.HifzProgramActivity" android:exported="false" />
        <activity android:name=".ui.HifzSetupActivity" android:exported="false" />
        <activity android:name=".ui.FreeMemActivity" android:exported="false" />
        <activity android:name=".ui.StudyReaderActivity" android:exported="false" />
        <activity android:name=".ui.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
