name: Parallel Hifz Core

on:
  push:
    branches: ["parallel/hifz-safeguard-sol"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hifz-core:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"
      - name: Run independent Hifz core tests
        run: gradle --no-daemon :hifz-core:test --stacktrace
      - name: Upload Hifz core test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: parallel-hifz-core-test-evidence
          path: |
            hifz-core/build/test-results/
            hifz-core/build/reports/tests/
          retention-days: 7
