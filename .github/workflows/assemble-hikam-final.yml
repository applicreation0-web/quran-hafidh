name: Reassemble reviewed Al-Hikam candidates

on:
  push:
    branches:
      - quran-safeguard-hikam-complete-corpus
    paths:
      - ".github/workflows/assemble-hikam-final.yml"

permissions:
  contents: write

jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reassemble reviewed candidates
        run: python3 scripts/assemble_hikam_candidates.py
      - name: Commit reviewed candidate assets and remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "quran-safeguard-ci"
          git config user.email "actions@users.noreply.github.com"
          git add app/src/main/assets/hikam/al_hikam_candidates.json
          git add app/src/main/assets/hikam/pending_verification.json
          git add app/src/main/assets/hikam/al_hikam_verified.json
          git rm .github/workflows/assemble-hikam-final.yml
          git commit -m "Reassemble reviewed Al-Hikam translations"
          git push origin HEAD:quran-safeguard-hikam-complete-corpus
