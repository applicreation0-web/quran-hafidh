name: Retired 0.10.7 reader-like refinement

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot 0.10.7 reader-like refinement has already been applied and tested."
