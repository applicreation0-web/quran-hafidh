name: Quran Hifz Astra source patch

on:
  push:
    branches:
      - work/hifz-0.7.3-astra-fix-all
    paths:
      - scripts/apply_hifz_astra_fixes.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply exact source patches
        run: python3 scripts/apply_hifz_astra_fixes.py
      - name: Commit patches
        shell: bash
        run: |
          set -euo pipefail
          git config user.name quran-hifz-ci
          git config user.email actions@users.noreply.github.com
          git add hifz-app/src/main/java/com/quransafeguard/hifz/preview/HifzPrefs.java \
                  hifz-app/src/main/java/com/quransafeguard/hifz/preview/HifzSessionActivity.java \
                  hifz-app/src/main/java/com/quransafeguard/hifz/preview/J10ReviewPlanner.java
          git diff --cached --quiet && exit 0
          git commit -m "fix(hifz): wire Astra priority and session budget fixes"
          git push origin HEAD:work/hifz-0.7.3-astra-fix-all
